services:
  - type: web
    name: miniapptrade8
    env: node
    buildCommand: npm install && npm run build
    startCommand: node server.js
    envVars:
      - key: VITE_OPENAI_API_KEY
        sync: false